png_recorder_node:
  ros__parameters:
    input_topic: /camera/camera/color/image_raw
    # Relative paths are resolved from the directory where ros2 launch is run.
    output_dir: docs/realsense_png
